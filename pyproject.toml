[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "template"
version = "0.0.1"
description = "A template for building python projects"
authors = [{name = "MG"}]
requires-python = ">=3.8"